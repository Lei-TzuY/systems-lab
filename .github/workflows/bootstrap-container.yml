name: Bootstrap mini-container-runtime import

on:
  push:
    branches:
      - main
    paths:
      - 'migration/bootstrap-container.json'

permissions:
  contents: write

jobs:
  import:
    runs-on: ubuntu-latest
    env:
      SOURCE_REPO: Lei-TzuY/mini-container-runtime
      SOURCE_SHA: 3b96aca6d23289147fe1f21132a4503edaf19a06
      MIGRATION_BRANCH: migration/import-mini-container-runtime
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Recheck exact frozen source and open PRs
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          trigger_sha="$(python3 -c 'import json; print(json.load(open("migration/bootstrap-container.json"))["source_sha"])')"
          test "$trigger_sha" = "$SOURCE_SHA"
          live_sha="$(curl -fsSL -H "Authorization: Bearer $GH_TOKEN" -H 'Accept: application/vnd.github+json' "https://api.github.com/repos/$SOURCE_REPO/branches/main" | python3 -c 'import json,sys; print(json.load(sys.stdin)["commit"]["sha"])')"
          test "$live_sha" = "$SOURCE_SHA"
          open_prs="$(curl -fsSL -H "Authorization: Bearer $GH_TOKEN" -H 'Accept: application/vnd.github+json' "https://api.github.com/repos/$SOURCE_REPO/pulls?state=open&per_page=100" | python3 -c 'import json,sys; print(len(json.load(sys.stdin)))')"
          test "$open_prs" = 0
      - name: Import preserved source history
        shell: bash
        run: |
          set -euo pipefail
          test ! -e projects/mini-container-runtime
          git config user.name "LeiZ"
          git config user.email "52287354+Lei-TzuY@users.noreply.github.com"
          git remote add container-source "https://github.com/$SOURCE_REPO.git"
          git fetch container-source --tags
          git cat-file -e "${SOURCE_SHA}^{commit}"
          git switch -c "$MIGRATION_BRANCH"
          git subtree add --prefix=projects/mini-container-runtime container-source "$SOURCE_SHA"
          git merge-base --is-ancestor "$SOURCE_SHA" HEAD
          source_tree="$(git rev-parse "${SOURCE_SHA}^{tree}")"
          subtree_tree="$(git rev-parse HEAD:projects/mini-container-runtime)"
          test "$source_tree" = "$subtree_tree"
          git push origin "$MIGRATION_BRANCH"
